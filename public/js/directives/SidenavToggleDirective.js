/**
 * Custom toggle button to control an <md-sidenav>. Inserts an element into DOM to shift the the contents of an 
 * <md-content> over by a certain amount, since the sidenav is position absolutely, it normally overlaps this content.
 * Animates on toggle close and toggle open.
 */
angular.module('SidenavToggleDirective', ['ngAnimate']).directive('iceSidenavToggle', function() {
	
})